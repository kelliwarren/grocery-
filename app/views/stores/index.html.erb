all my stores <br/>
<% @stores.each do |store| %>
store name: <%= link_to(store.name, user_store_path(store.user, store))%> <br />
<% end %>

<%= link_to('Add a store', new_user_store_path(current_user)) %>
