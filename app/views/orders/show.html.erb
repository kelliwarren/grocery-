Your current order
<% if @order.line_items.count == 0 %>
is empty.<br/>
<% else %>
contains:<br/>
<% end %>
<% @order.line_items.each do |item| %>
<% if item.product%>
<%= item.product.name %><%= item.quantity %>
<% else %>
<% end %>
<% end %>
<%= link_to("back to store", store_path(@order.store)) %><br/>
<% if @order.line_items.count > 0 %>
 <%= button_to('empty cart', user_order_path(@order.store.user,session[:order_id]),method: :delete) %>
<%= button_to('purchase', checkout_path(store_id: @order.store)) %>
<% end %>
