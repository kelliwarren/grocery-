one order here:
<% @order.line_items.each do |item| %>
<% if item.product%>
<%= item.product.name %><%= item.quantity %>
<%= link_to("back to store", store_path(item.product.store)) %><br/>

<% else %>
<% end %>
<% end %>
<%= button_to('empty cart', user_order_path(@order.store.user,session[:order_id]),method: :delete) %>
<%= button_to('purchase', user_order_path(@order.store.user,session[:order_id])) %>